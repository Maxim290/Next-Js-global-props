## Global Props in Next.js (`nextjs-global-props`)

### Features:

-   Fetch global data using `getInitialProps`
-   Global data available across all pages

### Installation:

```sh
npm install
```

### Run the project:

```sh
npm run dev
```
